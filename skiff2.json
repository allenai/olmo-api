{
    "projectName": "playground",
    "services": [
        {
            "name": "evaluations",
            "cwd": "./apps/evaluations",
            "dockerFile": "Dockerfile",
            "deploy": false,
            "extraBuildArgs": [
                "--platform", "linux/amd64",
                "--build-arg", "GITHUB_TOKEN=${GITHUB_TOKEN}"
            ]
        }
    ]
}