#!/bin/bash
set -euo pipefail

origin="${ORIGIN:-http://localhost:8080/v2}"
email="${1:-murphy@allenai.org}"
token=$(curl -sH "X-Auth-Request-Email: $email" "$origin/whoami" | jq -r .token)
echo "Authorization: Bearer $token"
